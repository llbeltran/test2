server:
  port: 8081
spring:
  cloud:    config:
      server:
        git:
          default-label: main2
          uri: https://github.com/llbeltran/test2
          search-paths: config-data
          ignore-local-ssh-settings: true
  application:
    name: config-service